// Custom cursor was retired — left as a no-op so existing imports continue to resolve.
// The design now relies on native cursors + clear focus/hover states.
export default function CustomCursor() {
  return null;
}
